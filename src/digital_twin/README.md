# Structure

./core holds the main structure of the digital twin
./hospital holds specific dataprepping and configs for this specific demo dataset
./main_cli is used to call the streamlit UI app - not available in public version